{
  "name": "hdb-planner",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev --",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "preview": "npm --prefix frontend run preview --",
    "data:recordbto": "node backend/scripts/backfill-recordbto.mjs",
    "data:refresh": "node backend/scripts/backfill-recordbto.mjs"
  }
}
